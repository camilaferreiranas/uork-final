<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uork.uork">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PerfilEmpresa"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".Carregamento">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastroEmpresa" />
        <activity android:name=".Proposta" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".PagamentoAssinatura" />
        <activity
            android:name=".Menu"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListaChat" />
        <activity android:name=".Chat" />
        <activity android:name=".CadastroUsuario" />
        <activity android:name=".CadastroProposta" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>